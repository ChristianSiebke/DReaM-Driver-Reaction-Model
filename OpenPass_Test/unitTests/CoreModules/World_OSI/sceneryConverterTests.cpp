#include "gtest/gtest.h"
#include "gmock/gmock.h"

#include "fakeRoad.h"

#include "WorldData.h"

using ::testing::Return;
using ::testing::ReturnRef;
using ::testing::SetArgReferee;
using ::testing::Invoke;
using ::testing::Const;
using ::testing::_;

TEST(AddLane, GivenRoadLane_IsAddedWithCorrectProperties_ReturnsTrue)
{
/*    FakeRoad fakeRoad;

    WorldData worldData;

    bool retVal = worldData.AddLane(*(fakeRoad.GetLaneSections().at(0)->GetLanes().at(0)));

    ASSERT_TRUE(retVal);
    ASSERT_EQ(0, 0); */
}
